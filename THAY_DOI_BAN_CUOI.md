# Thay đổi ở bản cuối

Phiên bản này thống nhất tên dự án thành:

**Hệ thống hỗ trợ xếp hạng khu vực ưu tiên rà phá bom mìn**

Các thay đổi chính:

- đổi tên dự án trên metadata, tiêu đề, đầu trang, chân trang và tài liệu chính;
- giao diện chính dùng tiếng Việt và tập trung vào kết quả xếp hạng;
- thay các nhãn tiếng Anh trên luồng demo bằng diễn giải tiếng Việt;
- giữ THOR, KH-9, WorldPop và OpenStreetMap dưới dạng tên riêng của nguồn dữ liệu;
- giữ cảnh báo rằng chỉ số xếp hạng không phải xác suất còn vật nổ;
- giữ bản đồ trực tuyến nhiều nguồn và chế độ dự phòng ngoại tuyến;
- bỏ thành phần giao diện kiểm chứng cũ không còn được dùng trong demo chính;
- thêm hướng dẫn triển khai Vercel;
- thêm `.vercelignore` để không đưa dữ liệu nguồn lớn và tệp trung gian vào bản triển khai;
- chuyển cấu hình Next.js về chế độ triển khai chuẩn của Vercel.
